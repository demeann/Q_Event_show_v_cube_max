"""Ядро приложения: конфиг, логирование, работа со временем.

Импортируйте модули явно, чтобы не тянуть лишние зависимости::

    from app.core.config import get_settings
    from app.core.logging import configure_logging
    from app.core.time import MSK, now_msk, to_msk
"""
